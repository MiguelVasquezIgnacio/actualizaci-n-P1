static class NumeroLiteral
{
    public static string Convertir(int numero)
    {
        // Agrega tu código aquí para convertir el número a su parte literal
        return "";
    }
}

class Program
{
    static void Main(string[] args)
    {
        int numero = 123;
        string literal = NumeroLiteral.Convertir(numero);
        Console.WriteLine(literal);
    }
}
